# Core Dependencies - Hardened Launch Version
streamlit>=1.38.0
pyyaml>=6.0
psutil>=5.9.0
chromadb>=0.5.0
numpy>=1.24.0
torch>=2.0.0
pandas>=2.0.0
requests>=2.0.0
huggingface_hub>=0.20.0

# Vector Embeddings & Search
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0          # Use faiss-gpu if you have CUDA

# vLLM for local inference (Quasar + fallback models)
vllm>=0.6.0

# Optional: Better performance / debugging
black
isort

# Optional frontier models - enable if you have API keys
openai>=1.40.0
anthropic>=0.34.0
google-generativeai>=0.8.0

# =============================================
# COMMENTED OUT - NOT USED IN CURRENT VERSION
# =============================================
# bittensor>=8.0.0          # Removed - running off-chain for now
# QuantumRingsLib[cpu]      # Removed - no longer quantum-specific

