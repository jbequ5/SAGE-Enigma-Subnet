# config/compute.yaml
# Toggle which Bittensor subnets to use for compute

chutes: true          # Private LLM inference (fast & cheap)
targon: false         # Secure TEE GPUs (for sensitive work)
celium: true          # Heavy parallel compute (for big swarms)
default_compute: "local"   # fallback
